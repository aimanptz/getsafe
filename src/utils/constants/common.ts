import { KEYBOARD_KEYS } from './keaboardKeys';

export const CONSTANTS = {
	noData: 'No data provided',
	nonDigitKeys: [
		KEYBOARD_KEYS.e,
		KEYBOARD_KEYS.dot,
		KEYBOARD_KEYS.comma,
		KEYBOARD_KEYS.dash,
	],
	loadingFallback: 'Loading...',
};
